name: Reusable Job
on : 
    workflow_call:
        #inputs:
jobs:
    deploy: 
        runs-on: ubuntu-latest
        steps: 
            - name: Deploy to production
              run: echo "Deploying to production..."